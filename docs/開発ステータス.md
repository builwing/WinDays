# 開発ステータス（再開ガイド）

最終更新: 2026-09-16

## 現在地: Phase 2（自動記録）— 本番反映済み（2026-09-11 サーバー時刻）。次は端末での実地確認と Phase 3

### Phase 0（準備）— 完了（2026-09-09 本番反映・実地確認済み）

要件は [要件定義書.md](要件定義書.md)。Phase の定義は同 §11。

### 完了

| 項目 | 状態 |
| --- | --- |
| 要件定義書 v0.2（確認事項の決定を反映） | ✅ |
| GitHub `builwing/WinDays`（private） | ✅ |
| メール送信基盤（VPS Postfix・sendmail・SPF/DKIM/DMARC pass 確認） | ✅ 本番稼働（2026-09-09） |
| WinTask 側バックエンド（ブランチ `feature/windays-phase0` → main にマージ） | ✅ 本番反映済み（migrate 1本・cache 再構築） |
| `api.days.winroad.org`（証明書・vhost） | ✅ 公開済み。`/up`=200・未認証 `/me`=401・CORS で `days.winroad.org` 許可 |
| WinDays Web 雛形（React + Vite + PWA。Hello ＋ API 疎通表示 ＋ `/email-verified`） | ✅ 公開済み。ブラウザから API 疎通 OK |
| `days.winroad.org`（証明書・vhost） | ✅ 公開済み |
| 法的ページ（WinTask `landing/`）に WinDays を追記 | ✅ main に反映（静的配信のため公開済み） |
| キューワーカー systemd unit（WinTask `deploy/install-queue-worker.sh`） | ✅ 任意。`.env` は当面 `sync` |

### 実地確認（2026-09-09）

`client=days` で登録 → 「【WinDays】メールアドレスの確認」が到達 → リンクで `days.winroad.org/email-verified?status=ok` に戻り「メールアドレスを確認しました」を表示。テストアカウント id 19（`wingnakada+days@gmail.com`）は残置。WinTask 側の作業 worktree `/var/www/WinTask-days` は削除済み。

### 本番反映の手順（記録。次回以降のバックエンド反映は同じ流れ）

```bash
cd /var/www/WinTask && sudo bash deploy/windays-phase0-deploy.sh   # ff-merge + migrate + cache
cd /var/www/WinTask && sudo bash deploy/windays-api-domain.sh      # api.days.winroad.org 証明書 + vhost
cd /var/www/WinDays && bash deploy/web-reflect.sh                  # web/build を作成
cd /var/www/WinDays && sudo bash deploy/days-domain.sh             # days.winroad.org 証明書 + vhost
```

注意: `windays-phase0-deploy.sh` の autoload 再生成は `hide` で実行する（修正済み）。`/up` は CORS 対象外なのでブラウザからの疎通確認は `/api/v1/me`（401）を使う。

### 残っている確認事項

- J-PlatPat（9 類・42 類）の「WinDays」商標検索（公開前に必要）
- GA4 の測定 ID（LP を作る Phase 1 で `web/.env.production` の `VITE_GA_MEASUREMENT_ID` に設定）
- Xserver サポートへの遮断解除依頼（メールは Postfix で解決したため任意）
- Cookie 同意バナーの文言（Phase 1）

## Phase 1 の進捗（2026-09-09）

- バックエンド: `feature/windays-phase1`（push 済み・**未デプロイ**）。カテゴリ / タイムログ / ダッシュボード / 計測 / いつでもメモ（`days_memos`）。test 345 パス。反映は `sudo bash /var/www/WinTask-days/deploy/windays-phase1-deploy.sh`（migrate 2 本）。
- フロント: 本番反映済み（LP・登録/ログイン・今日・ふりかえり・カテゴリ・設定・メモ・マニュアル `/manual.html`）。バックエンド未反映のため、ログイン後のデータ取得は反映後に動く。
- DNS: `days` の明示レコード追加でワイルドカードが `api.days` に効かなくなり、`api.days` の A レコードを追加（2026-09-09）。ネガティブキャッシュ 1 時間。
- 別件: `task.winroad.org` の証明書が 9/4 に期限切れ（renewal の webroot が Flutter 側のまま）。`sudo certbot renew --cert-name task.winroad.org --webroot -w /var/www/WinTask/web/build` で修正。

## 要件定義書を v0.3 に再構築（2026-09-11）

ユーザー要望「予定管理と行動管理を 1 つに。予定の開始時刻で自動的に記録を開始し、終了時刻で確認、3 分放置で予定どおり終了」を中核コンセプトにして要件定義書を再構築した。

- 中核: **予定駆動の自動記録**（§4.0・§4.4）。判定はサーバーの毎分スケジューラ `days:auto-track` と状態機械 `days_plan_runs`。通知やアプリの状態に依存しない
- 予定: WinTask の個人予定を共用（実装済み）＋ 繰り返し予定（`rrule` を 14 日先まで展開）＋ 初回設定ウィザード。習慣・ルーティンは繰り返し予定に統合（`days_habits`・`days_templates` 廃止）
- ネイティブ: **Flutter で別実装せず Capacitor で同じ React コードを配信**（§16）。Phase 4
- フェーズを組み直し: Phase 2 ＝ 自動記録（中核）、Phase 3 ＝ 習慣・サマリ・導線、Phase 4 ＝ ネイティブ・Google 連携
- 2026-09-11 に確定: 開始・終了とも猶予 3 分（放置後は予定時刻で確定）、習慣・ルーティンは繰り返し予定に統合、ネイティブは Capacitor、位置スタンプは Phase 4 候補に残す（無操作の GPS 自動取得は不採用・§16.1）
- 繰り返し予定の整合案を承認済み。WinTask `docs/予定・Googleカレンダー連携_要件定義書.md` v0.3 §15 に追記（`feature/windays-plans` にコミット）。実装は WinTask `feature/calendar-recurrence` → WinDays の順

## Phase 2-1: 繰り返し予定（2026-09-12 実装・本番反映済み）

- WinTask `feature/calendar-recurrence`（worktree `/var/www/WinTask-days`。`feature/windays-plans` の上に積んである）: `calendar_events` に `recurrence_parent_id` / `original_starts_at` / `is_exception` / `skip_weekends` / `skip_holidays` / `nonworking_action` / `days_auto_track`、`CalendarRecurrenceService`（14 日展開・冪等・scope=this/following/all・週末祝日スキップ/シフト）、`calendar:expand-recurring`（毎日 00:15 JST）、`/me/events`・`/teams/{team}/events`・`/days/plans` の rrule 対応とマスター除外、`GET /days/plans/recurring`、`POST /days/plans/{id}/skip`。test 365 パス
- WinDays: 予定シートに「繰り返し」（毎日／平日／毎週（曜日）／毎月・土日祝スキップ）、回の編集時に反映範囲（この回のみ／以降すべて／すべて）、タイムラインの ↻、設定に繰り返し予定の一覧と削除、マニュアル 06 に追記
- 反映（この順）: `sudo bash /var/www/WinTask-days/deploy/calendar-recurrence-deploy.sh`（plans の migrate も含む。`windays-plans-deploy.sh` は不要になった）→ `bash deploy/web-reflect.sh`
- 本番の cron で `schedule:run` が毎分動いていることが前提（`deploy/install-scheduler.sh` 参照）

## Phase 2-2: 自動記録（2026-09-13 実装・本番反映済み）

- WinTask `feature/windays-autotrack`（`feature/calendar-recurrence` の上）: `days_plan_runs`（状態機械）・`days_notifications`・`days_segments.plan_id`/`edited_at`・users の `days_auto_track`/`days_grace_minutes`/`days_paused_on`、`DaysAutoTrackService`（毎分 `tick`: 開始確認→猶予後に予定時刻で開始→終了確認→猶予後に予定時刻で終了。同カテゴリ引き継ぎ・別カテゴリ停止と `previous_segment_id`・埋まっている区間の回避・接した予定の切替）、操作 `ok/change/shift/skip/resume_previous/continue/extend/stop_now`、`GET /days/plan-runs?date=`・`POST /days/plan-runs/{id}/act`・`GET|PUT /days/settings/auto-track`・`GET /days/notifications`、`days:auto-track` 毎分（withoutOverlapping）。test 380 パス
- WinDays: 「今日」の最上部に確認カード（`ConfirmCards`）、「今日の予定」リスト（`TodayPlans`・状態バッジ）、30 秒ごとの状態更新、記録編集シートの「予定から自動記録」表示、設定に自動記録（ON/OFF・猶予・今日は停止）、マニュアル 06 に追記
- 反映（この順・3 ブランチまとめて）: `sudo bash /var/www/WinTask-days/deploy/windays-autotrack-deploy.sh` → `bash deploy/web-reflect.sh`。**cron の `schedule:run`（毎分）が必須**（スクリプトが有無を表示する。無ければ `deploy/install-scheduler.sh`）
- 未実装: Web Push（現状はアプリ内カードのみ。閉じている間の「入口」は無いが記録は正しい）、`revert`（戻す）、予定ごとの自動記録 OFF の UI、初回設定ウィザード、予定どおり率

## Phase 2-3: Web Push（2026-09-14 実装・本番反映済み）

- WinTask `feature/windays-webpush`（`feature/windays-autotrack` の上）: `minishlink/web-push`（gmp/bcmath 無しでも動く。通知は `DaysPushService` で握りつぶす）、`config/days.php`（`VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `VAPID_SUBJECT`）、`days_push_subscriptions`・`days_notifications.pushed_at`、`DaysPushService`（404/410 で購読削除）、`SendDaysPush`（ShouldQueue）、`DaysAutoTrackService::notify` から購読があればキューへ、`GET /days/push/public-key`・`GET|POST|DELETE /days/push-subscriptions`・`POST /days/push/test`、`days:vapid-keys --write`。test 385 パス
- WinDays: `public/push-sw.js`（Workbox の SW に `importScripts`。push 表示と通知タップ／操作ボタン → `/app?run=&act=`）、`lib/push.ts`、設定の「通知（Web Push）」（購読・解除・テスト・iOS の案内）、「今日」で通知パラメータの実行と未購読の案内
- 反映（この順・4 ブランチまとめて）: `sudo bash /var/www/WinTask-days/deploy/windays-webpush-deploy.sh`（composer install・VAPID 鍵の生成と .env 追記・migrate 4 本）→ `bash deploy/web-reflect.sh`。本番は `QUEUE_CONNECTION=database` ＋ `wintask-queue` が動いていること（スクリプトが状態を表示）
- 確認手順: 設定 > 通知 > 「この端末で通知を受け取る」→「テスト送信」。iPhone はホーム画面に追加したアイコンから開く

## Phase 2-4: 初回設定ウィザード（2026-09-15 実装・本番反映済み）

- WinTask `feature/windays-onboarding`（`feature/windays-webpush` の上）: `users.days_onboarded_at`、`POST /days/onboarding`（最大 5 件の繰り返し予定を `CalendarRecurrenceService::createMaster` で一括作成し完了を記録。空でも完了＝スキップ）。test 388 パス
- WinDays: `/app/welcome`（4 画面: 平日の仕事／睡眠／休日の定番／まとめ＋自動記録の説明＋通知の許可）、「今日」は `user.days_onboarded_at` が空ならウィザードへ誘導（既存ユーザーも初回 1 度だけ出る。スキップ可）、予定シートに「この予定を自動記録する」、マニュアル 01 に追記
- 反映（この順・5 ブランチまとめて）: `sudo bash /var/www/WinTask-days/deploy/windays-onboarding-deploy.sh` → `bash deploy/web-reflect.sh`

## Phase 2-5: 予定どおり率・予実差分（2026-09-16 実装・本番反映済み）

- WinTask `feature/windays-adherence`（`feature/windays-onboarding` の上）: `DaysStatsService` に `adherence`（予定本数・予定分数・同カテゴリで重なった実績分数・`rate`・`runs`＝自動記録の結果内訳）と `variance`（カテゴリ別の実績−予定・`plan_days`）を追加。週の `days[]` に `adherence_rate`。対象はカテゴリ付きの個人予定（マスター・削除済み除外）、「今」より先は分母に入れない。**既存バグ修正**: 週のカテゴリ合算が配列の `+` で初日分しか残らなかった（週合計・領域・記録合計に影響していた）。test 392 パス
- WinDays: ふりかえりに「予定どおり率（週）」（大きな数字・曜日ごとの棒・自動記録の内訳・予実差分の 1 日平均）、日の内訳にその日の予実差分、マニュアル 07 に追記
- 反映（この順・6 ブランチまとめて。migrate 追加なし）: `sudo bash /var/www/WinTask-days/deploy/windays-adherence-deploy.sh` → `bash deploy/web-reflect.sh`

## Phase 2-6: 止め忘れ通知（2026-09-16 実装・本番反映済み）

- WinTask `feature/windays-overrun`（`feature/windays-adherence` の上）: `users.days_overrun_hours`（既定 4・0=OFF。migrate 1 本）、`DaysOverrunService::tick`（進行中の記録が閾値を超えたら「終了／続ける」の Push、購読が無ければメール `DaysOverrunNotification`。再通知は H・3H・7H の最大 3 回。起点はワンタップ計測なら開始時刻、「続ける」後の自動記録なら予定の終了時刻。予定の時間内と +N 分の延長中は対象外）、`days:notify-overrun`（5 分ごと）、設定 `overrun_hours`（0〜12）、`POST /days/segments/{id}/stop` の `notification_id`（通知からの終了を `acted_at`/`action=stop` に記録＝「通知からの終了率」の材料）。test 397 パス
- WinDays: `push-sw.js` の止め忘れ通知（「終了」→ `/app?seg=&act=stop&n=`、「続ける」は閉じるだけ）、「今日」で `seg`/`act=stop` を実行、進行中バーに閾値超えの表示（ワンタップ計測のみ）、設定 > 自動記録に「止め忘れ通知」（OFF／1〜12 時間）、マニュアル 06・10 に追記
- 反映（この順・7 ブランチまとめて。migrate 6 本）: `sudo bash /var/www/WinTask-days/deploy/windays-overrun-deploy.sh` → `bash deploy/web-reflect.sh`。メールは本番の Postfix（`MAIL_MAILER=sendmail`）で送る

## Phase 2-7: ショートカット起動（2026-09-16 実装・本番反映済み）

- WinTask `feature/windays-quick`（`feature/windays-overrun` の上）: `days_categories.quick_slot`（migrate 1 本）、`GET|PUT /days/quick-slots`（未割り当ての枠は並び順の N 番目で代用）、`POST /days/segments/quick`（`start=<id|name>` / `slot=N` / `stop=1`。同じカテゴリが進行中なら noop、別なら前を終了して開始。冪等）。test 400 パス
- WinDays: `/quick` ページ（`slot`・`start`・`stop`・`memo` を実行して「今日」へ遷移しトースト。未ログインなら `sessionStorage` に保留してログイン／登録後に実行）、マニフェストの `shortcuts`（記録を終了・クイック 1〜3・メモを書く）、設定 > ショートカット（枠のカテゴリ選択・URL の案内）、マニュアル 08・10 に追記
- 反映（この順・8 ブランチまとめて。migrate 7 本）: `sudo bash /var/www/WinTask-days/deploy/windays-quick-deploy.sh` → `bash deploy/web-reflect.sh`。shortcuts はマニフェスト更新後、Android は再インストール（ホーム画面から削除→追加）で反映されることがある

## Phase 2 の本番反映（2026-09-11 サーバー時刻・確認済み）

- WinTask main は `feature/windays-quick`（008eae6）に ff-merge 済み。migrate は 2026_09_16_000002 まで全て Ran。VAPID 鍵は `.env` に生成済み
- サーバーからの疎通（sudo 不要で確認）: 新 API（`days/dashboard`・`quick-slots`・`settings/auto-track`・`plan-runs`・`push/public-key`・`POST segments/quick`・`POST onboarding`）は未認証で 401。`days.winroad.org/quick?slot=1` は 200、マニフェストの shortcuts 5 件・`push-sw.js` の止め忘れ処理・マニュアルの追記が配信されている
- スケジューラ: `/etc/cron.d/wintask` で `schedule:run` が毎分。`schedule:list` に `days:auto-track`（毎分）・`days:notify-overrun`（5 分ごと）・`calendar:expand-recurring`（00:15）。`schedule.log` で auto-track の実行を確認。`laravel.log` に当日のエラーなし
- キュー: `QUEUE_CONNECTION=sync`・`wintask-queue` は inactive。Push は auto-track の中で同期送信される（動作に支障なし。Push の送信遅延が気になれば `deploy/install-queue-worker.sh` ＋ `QUEUE_CONNECTION=database`）
- 後片付け: worktree `/var/www/WinTask-days` を除去（コピーした `.env` も消えた）。feature ブランチは main に含まれるので残置
- **未 push**: WinTask main（origin/main より 12 コミット先）・WinDays main（origin/main より 11 コミット先）。`git push` はユーザー判断

### 端末での実地確認（残り。ユーザー作業）

1. ログイン → 初回設定ウィザード（`/app/welcome`）で平日の仕事・睡眠を登録 → 翌日の自動記録と確認カード
2. 設定 > 通知 > 「この端末で通知を受け取る」→「テスト送信」（iPhone はホーム画面に追加したアイコンから）
3. ふりかえりに「予定どおり率（週）」と予実差分が出る
4. ワンタップ計測を 4 時間以上放置 → 止め忘れの Push（購読が無ければメール）が届き、「終了」で止まる
5. ホーム画面アイコンの長押しで shortcuts（記録を終了・クイック 1〜3・メモ）。出ない場合はアイコンを削除して追加し直す

## 次にやること: Phase 3（続ける仕組みと導線）

要件定義書 §11 の Phase 3。着手順の案:

1. 習慣 = 繰り返し予定の達成判定・ストリーク（`calendar_events.is_habit`・`habit_target_minutes`・`days_habit_logs`・`GET /days/habits`）
2. 1 日サマリの自動下書き（WinTask `diary_entries`・`team_id=null`）と気分
3. 週次振り返りメール（毎週日曜夜・ON/OFF）と朝の「今日の予定」通知
4. 予実差分からの予定調整提案（「予定を 18:30 終了に直しますか？」）
5. `revert`（自動記録を戻す）・帯のドラッグ編集・CSV・無料上限ゲート

（旧メモ）
1. ~~WinTask `feature/calendar-recurrence`~~（済）
2. WinTask `feature/windays-autotrack`: `days_plan_runs`・`days_segments.plan_id`／`source=auto`・`days:auto-track`（毎分）・確認カード操作 API（`/days/plan-runs/{id}/act`）・`GET /days/today`・設定 API・Web Push
3. WinDays: 「今日」画面の再設計（確認カード＋今日の予定リスト）、繰り返し設定 UI、初回設定ウィザード、自動記録の設定画面、Push 購読

## 予定（プラン）を前倒しで実装（2026-09-11）

要件定義書 §4.4 の予定を Phase 2 から前倒し。**実体は WinTask の個人予定 `calendar_events` を共用**（`days_plans` は新設しない。決定の経緯は §15.1）。

- バックエンド: `feature/windays-plans`（worktree `/var/www/WinTask-days`）。`calendar_events.days_category_id` 追加（migrate 1 本）、`/api/v1/days/plans`（一覧・作成・更新・削除・`copy-to-actual`）。test 354 パス。反映は `sudo bash /var/www/WinTask-days/deploy/windays-plans-deploy.sh`。
- フロント: 「今日」に予定の追加ボタン（＋の上のカレンダー）、予定シート（タイトル・カテゴリ任意・時刻・メモ・実績にコピー・削除）、タイムラインの 2 列表示（予定｜実績）。設定とマニュアル（06 予定を立てる）に「予定は WinTask と共通」を明記。
- 未着手: ダッシュボードの「予定どおり率」、習慣からの予定自動生成（Phase 2）。

## Phase 2 に追加した要件（2026-09-09）

止め忘れ通知（Web Push＋メール、閾値既定 4 時間、再通知は倍間隔で最大 3 回）と、ショートカット起動（PWA `shortcuts`・`/quick?start=`／`stop=1`・クイック枠 3 件）を Phase 2 の先頭に入れた。需要の裏づけとして「記録の平均継続時間」「終了時刻をあとから修正した割合」「通知からの終了率」を計測し、数字次第で Live Activities を持つネイティブ版を検討する。詳細は要件定義書 §4.3・§4.6・§8・§9・§11。

## 次にやること: Phase 1（MVP）

共通アカウント登録/ログイン画面、カテゴリ、タイムログ（ワンタップ＋帯入力）、日の円グラフ、週の領域比率、計測イベント、LP。バックエンドは `/api/v1/days/*`（`days_categories` / `days_segments` など）を WinTask 側に追加する。
