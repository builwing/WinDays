# API のベース URL（末尾スラッシュなし）。本番は api.days.winroad.org、ローカルは php artisan serve。
VITE_API_ORIGIN=https://api.days.winroad.org
# WinTask（同一アカウントで遷移する先）
VITE_TASK_URL=https://task.winroad.org
# GA4 測定 ID（未設定なら読み込まない）
VITE_GA_MEASUREMENT_ID=
